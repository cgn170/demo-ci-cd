mode: Mainline
tag-prefix: "[vV]"
commit-message-incrementing: MergeMessageOnly
branches:
  bugfix:
    regex: bugfix?[/-]
    source-branches: ["main"]
  feature:
    increment: Minor
    regex: feature?[/-]
  main:
    is-mainline: true
    increment: Patch
    regex: main$
