mode: ContinuousDeployment
tag-prefix: "[vV]"
commit-message-incrementing: MergeMessageOnly
branches:
  main:
    regex: ^main$
    increment: Patch
  feature:
    regex: ^feature[/-]
    increment: Minor
    source-branches: ["main"]
  bugfix:
    regex: ^(bugfix|fix)[/-]
    increment: Patch
    source-branches: ["main"]
